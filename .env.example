# Frontend build-time vars (Vite). Copy to `.env.local` for local dev.
# Adsterra ad master switch. Set to "true" on production to load real ads
# (each slot also needs its key filled in src/AdSlot.tsx → AD_UNITS).
# Leave unset/empty → no real ads; local dev always shows placeholders.
VITE_ADS_ENABLED=
