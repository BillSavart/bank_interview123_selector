# LLM proxy keys for LOCAL dev. Copy to `server/.env` and fill in.
# (On the VM these live in /etc/bank-interview/proxy.env instead.)
GEMINI_API_KEY=
GROQ_API_KEY=
# optional overrides:
# GEMINI_MODEL=gemini-2.5-flash
# GROQ_MODEL=llama-3.3-70b-versatile
# PORT=3001
